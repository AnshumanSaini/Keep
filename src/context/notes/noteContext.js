import { createContext } from "react";

//We created a new context. The following context will hold all the states related to notes.
const noteContext = createContext();

export default noteContext;

